apiVersion: v1
kind: Service
metadata:
  name: {{ include "license-manager.fullname" . }}
  labels:
    {{- include "license-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "license-manager.selectorLabels" . | nindent 4 }}
